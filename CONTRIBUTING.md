# 贡献指南

提交较大的功能改动前，建议先发 Issue 说明使用场景与方案。

1. Fork 仓库，从 `main` 创建分支。
2. 按[开发指南](docs/开发指南.md)准备环境，让每次修改只解决一个问题。
3. 保留中文界面、数据兼容性与恢复能力；普通归类应继续保留原文件位置。
4. 运行对应检查。涉及桌面交互时，在解锁的桌面执行完整验收；说明跳过项。
5. 提交 Pull Request，写明问题、变化、验证结果。界面调整请附不含个人信息的截图。

遵循现有 C# / WPF 风格与 `.editorconfig`。版本统一在 `Directory.Build.props` 中管理。
请勿提交 `artifacts`、本地 SDK、运行数据、密钥、日志、个人路径或编辑器配置。
公开测试仅使用虚构文件和本地模拟服务，不依赖真实 AI 密钥。

参与讨论时请保持尊重，围绕可复现的问题与具体方案交流。提交代码表示你有权按本项目 MIT 许可证贡献这些内容。
